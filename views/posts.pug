extends layout 

block content 
  h1= title 
  if posts
    h2 ALL POSTS
    //- p #{posts}
    for post in posts
      div
        //- p #{post._id}
        h2= post.title
        p= post.content
        p= post.date
        p= post._id
        a(href='/posts/' + post._id) LINK TO POST PAGE